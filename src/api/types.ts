export interface User {
    id: string;
    firstName: string;
    lastName: string;
    createdAt: string;
    updateAt: string;
}

export interface Credentials {
    email: string;
    password: string;
}
