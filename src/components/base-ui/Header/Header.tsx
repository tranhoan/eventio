import React from 'react';
import { EventioLogo, HeaderElement } from './style';

export const Header = () => {
    return (
        <HeaderElement>
            <EventioLogo />
        </HeaderElement>
    );
};
